import styled from "styled-components";

export const Container = () => {
    return (
        <SContainer>

        </SContainer>
    )
}

export const SContainer = styled.div`
  max-width: 1440px;
  margin: 0 auto;
  padding-left: 150px;
  padding-right: 150px;
  min-height: inherit;
`